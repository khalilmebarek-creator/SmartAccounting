# PERFORMANCE_OPTIMIZATION.md
## دليل تحسين الأداء
### Smart Accounting Platform v2.5.0

---

## 1. أفضل الممارسات لتحسين الأداء

### قاعدة ذهبية

> ⭐ **لا تحسّن ما لا تقيسه.** استخدم مؤشرات الأداء لتحديد المشاكل أولاً.

### مبادئ التحسين

1. **التحسين قبل التوسع**: حسّن الكود قبل إضافة ميزات جديدة
2. **القياس أولاً**: قِس الأداء قبل وبعد كل تحسين
3. **التبسيط**: الكود الأبسط هو الأسرع عادةً
4. **الكاش ذكي**: اخزن النتائج المحسّنة لتجنب إعادة الحساب

---

## 2. تحسين سرعة قاعدة البيانات

### الفهرسة (Indexing)

**المشكلة**: استعلامات بطيئة على جداول كبيرة

**الحل**: إضافة فهارس (Indexes) على الأعمدة المستخدمة في البحث

```
# مثال: فهرسة عمود التاريخ
CREATE INDEX idx_analysis_date ON analysis_results(analysis_date);

# مثال: فهرسة مركّب
CREATE INDEX idx_analysis_user_date ON analysis_results(user_id, analysis_date);
```

**النتيجة**: تسريع الاستعلامات 5-50x حسب حجم الجدول

---

### WAL Mode (Write-Ahead Logging)

النظام يستخدم WAL mode بالفعل. هذا يحسن:
- **سرعة القراءة**: العمليات لا تتداخل
- **الاستقرار**: fewer database locks
- **الأداء المتعدد**: دعم مستخدمين متعددين

**التحقق**:
```sql
PRAGMA journal_mode;
-- يجب أن يُظهر: wal
```

---

### تحسين الاستعلامات

**تجنب `SELECT *`**:
```
# سيء
SELECT * FROM transactions;

# جيد
SELECT id, amount, date FROM transactions WHERE date > '2025-01-01';
```

**استخدم `EXPLAIN QUERY PLAN`**:
```
EXPLAIN QUERY PLAN SELECT * FROM transactions WHERE amount > 1000000;
-- يُظهر خطة التنفيذ ويساعد في اكتشاف المشاكل
```

---

### التحليل التلقائي

شغّل `ANALYZE` دورياً لتحديث إحصائيات قاعدة البيانات:
```
ANALYZE;
```

⭐ **نصيحة**: شغّل `ANALYZE` بعد إدخال كمية كبيرة من البيانات.

---

## 3. تحسين واجهة المستخدم

### تقليل إعادة الرسم

**المشكلة**: إعادة رسم كاملة للواجهة عند كل تغيير بسيط

**الحل**: تحديث فقط العناصر المتغيرة
```
# بدلاً من إعادة بناء كل الجدول
results_table.updateRow(index, new_data)

# بدلاً من إعادة رسم كاملة للرسم البياني
canvas.draw_idle()  # فقط عند الانتهاء
```

---

### threading للحسابات الثقيلة

**المشكلة**: تجميد الواجهة أثناء الحسابات

**الحل**: استخدام QThread
```
# إنشاء خيط منفصل للحساب
worker = CalculationWorker(data)
worker.finished.connect(on_calculation_done)
worker.start()
```

**النظام الحالي يستخدم**:
- `_BackgroundCheckWorker` في data_entry.py لفحص الاحتيال
- `AIWorker` في chat_view.py للذكاء الاصطناعي

---

### تحسين الرسوم البيانية

**المشكلة**: الرسوم تستغرق وقتاً طويلاً

**الحلول**:
```
# 1. استخدم DPI منخفض
figure = Figure(figsize=(8, 4), dpi=80)  # بدلاً من 100

# 2. أغلق الرسوم غير المستخدمة
plt.close(figure)

# 3. استخدم绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑绑
# 4. تجنب إعادة الرسم المتكرر
```

---

### إدارة الذاكرة

**مؤشرات التسريب**:
- زيادة استخدام RAM مع الوقت
- بطء تدريجي
- `RuntimeWarning: leak`

**الحلول**:
```
# تأكد من إغلاق الرسوم
plt.close('all')

# استخدم __del__ إذا لزم الأمر
def __del__(self):
    if hasattr(self, 'figure'):
        plt.close(self.figure)
```

⭐ **النظام الحالي**: يستخدم `plt.close(self.figure)` في `clear_chart()` و `_clear_all()`.

---

## 4. تقليل استهلاك الذاكرة

### مراقبة الاستهلاك

```
# للتحقق من استخدام الذاكرة
import psutil
import os

process = psutil.Process(os.getpid())
memory_mb = process.memory_info().rss / 1024 / 1024
print(f"Memory: {memory_mb:.1f} MB")
```

### حدود الاستهلاك

| المكون | الحد الأقصى الموصى به |
|--------|---------------------|
| البرنامج كاملاً | < 150 MB |
| استعلام واحد | < 10 MB |
| رسم بياني واحد | < 5 MB |
| تقرير PDF | < 20 MB |

### تقنيات تقليل الذاكرة

1. **تحميل متأخر (Lazy Loading)**: لا تحمل البيانات إلا عند الحاجة
2. **تصفح الصفحة (Pagination)**: لا تعرض كل البيانات دفعة واحدة
3. **حذف الكاش**: احذف البيانات القديمة من الكاش
4. **إغلاق الموارد**: تأكد من إغلاق ملفات قاعدة البيانات

---

## 5. نصائح لإدارة البيانات الكبيرة

### عند وصول قاعدة البيانات لـ 100MB+

1. **تنقية البيانات القديمة**:
   - احذف التحليلات القديمة (أكثر من سنة)
   - أرشفة البيانات في ملفات منفصلة

2. **تقسيم الجداول (Partitioning)**:
   - قسّم جدول transactions حسب السنة
   - يسرّع الاستعلامات على فترات محددة

3. **تحديث الإحصائيات**:
   - شغّل `ANALYZE` بعد التنقية
   - أعد بناء الفهارس

---

### عند وجود أكثر من 10 مستخدمين

1. **استخدم قاعدة بيانات سيرفر**: PostgreSQL بدلاً من SQLite
2. **فعّل connection pooling**: لتقليل اتصالات قاعدة البيانات
3. **راقب الأداء**: سجّل وقت الاستعلامات

---

## 6. مؤشرات الأداء (KPIs)

### مؤشرات يجب مراقبتها

| المؤشر | الهدف | كيفية القياس |
|--------|-------|-------------|
| وقت تحميل الشاشة | < 3 ثوانٍ | timer عند الفتح |
| وقت استعلام واحد | < 200ms | EXPLAIN QUERY PLAN |
| وقت تصدير PDF | < 10 ثوانٍ | timer عند التصدير |
| استهلاك RAM | < 150 MB | psutil |
| وقت بدء التشغيل | < 5 ثوانٍ | timer عند التشغيل |

### أدوات القياس

**أداة مدمجة**:
```
python -c "
import time
from database.db_connection import DatabaseConnection

start = time.time()
db = DatabaseConnection()
db.connect()
elapsed = time.time() - start
print(f'DB Connect: {elapsed*1000:.1f}ms')
db.disconnect()
"
```

**مراقبة مستمرة**:
- سجّل الأداء في ملف logging
- راقب التغيرات عبر الفترات
- حدد المشاكل قبل أن تتفاقم

---

## 7. قائمة التحسينات حسب الأولوية

### عالية الأولوية (تؤثر بشكل كبير)

| # | التحسين | التأثير المتوقع |
|---|---------|----------------|
| 1 | فهرسة الجداول | 5-50x استعلامات أسرع |
| 2 | threading للحسابات | لا تجميد للواجهة |
| 3 | إغلاق الرسوم البيانية | تقليل 30% ذاكرة |

### متوسطة الأولوية

| # | التحسين | التأثير المتوقع |
|---|---------|----------------|
| 4 | تحسين الاستعلامات | 2-5x أداء |
| 5 | كاش ذكي | تقليل استعلامات متكررة |
| 6 | تقليل DPI الرسوم | 20% أداء |

### منخفضة الأولوية

| # | التحسين | التأثير المتوقع |
|---|---------|----------------|
| 7 | تحسين الـ imports | تقليل بدء التشغيل |
| 8 | ضغط البيانات | تقليل مساحة القرص |
| 9 | تحسين الرسائل | تجربة مستخدم أفضل |

---

## 8. روابط ذات صلة

- [解决问题](TROUBLESHOOTING.md) - حل المشاكل الشائعة
- [دليل التثبيت](INSTALLATION_GUIDE.md) - المتطلبات الأساسية
- [النشر والتفعيل](DEPLOYMENT_GUIDE.md) - نشر التطبيق

---

*آخر تحديث: يوليو 2026*
*الإصدار: v2.5.0*
