# Surgical Editing Protocol (بروتوكول التعديل الجراحي)

## الدور والمهمة
Staff Software Engineer. التعديلات تكون جراحية دقيقة دون تخريب الميزات الأخرى.

## قواعد التعديل الجراحي
1. **المس فقط ما يجب لمسه**: لا تحسن تنسيق كود مجاور، لا تعد صياغة تعليقات قديمة، لا تقم بـ Refactoring لكود يعمل ما لم يُطلب.
2. **مطابقة الأسلوب**: التزم بأسلوب الكود الحالي تماماً حتى لو كان غير مثالي.
3. **تنظيف مخلفاتك فقط**: إذا تسبب تعديلك في جعل دالة أو Import "يتيماً"، فقم بإزالته. لا تلمس الأكواد الميتة القديمة.

## بروتوكول التحليل والتنفيذ
1. **تحليل التأثير (Impact Analysis)**: اقرأ PROJECT_MAP.md، حدد الملفات المتأثرة بدقة.
2. **السلامة المعمارية والتجريد**: التزم بـ DRY، استخدم طبقة Shared/Core، أضف Logging للتعديل الجديد.
3. **التحقق والنجاح (Goal-Driven)**: اكتب الاختبار، تأكد من فشله، ثم اجعله ينجح (TDD). تأكد من عدم كسر الاختبارات القديمة.
4. **مزامنة الحالة**: حدّث PROJECT_MAP.md فوراً. أي كود أصبح Deprecated يجب أن يُعالج أو يُسجل.

## قواعد الرفع
- بعد كل تعديل كود، تحديث الموقع (docs/) مطلوب
- بعد كل تعديل، تحديث PROJECT_MAP.md مطلوب
- الإصدار الحالي: v3.2.0

## الحالة الحالية (2026-08-16)
- **جلسة توحيد تصميم الشاشات المحاسبية الأربع (2026-08-16):** الشاشات 34-37 (الرواتب/الميزانية/المشتريات/الفواتير الإلكترونية) — نمط الجلسة 85 (عناوين فوق الحقول + صفوف مقسمة + زر محاذاة أسفل): payroll 2 صفّان + run بعنوانين، budgeting 2 صفّان، procurement صفّان + إزالة stats.addStretch، einvoicing صفّان + إزالة stats.addStretch — TDD: `test_form_fields_have_labels` لكل شاشة + فئتين جديدتين TestProcurementView + TestEInvoicingView → **2071 اختباراً أخضر** + PROJECT_MAP (جلسة 96) — **مكتمل**: كل الشاشات المحاسبية 30-37 موحّدة
- **جلسة دليل تفاعلي + توزيع الشاشات (2026-08-16):** Help ← «دليل الاستخدام» (7 تبويبات ×3 لغات RTL بلا كود/بنية — اختبار يرفض تسريب الكود) + **توزيع الشاشات لكل عضو**: المدير يحدد شاشات كل عضو من الإعدادات (مجموعة ظاهرة للمدير فقط + حوار 39 checkbox، الشاشتان 1و2 إلزاميتان) — الشريط يُرشّح وحراسة `_go_to_view` ترفض الممنوع + سجل مركزي `view_registry.py` → **2061 اختباراً أخضر** + PROJECT_MAP (جلسة 95) — **مكتمل**
- **جلسة تنبيه يوم التسديد + تدقيق أمني (2026-08-13):** جوال: تنبيهان لكل استحقاق (تذكير 3 أيام + يوم التسديد نفسه 09:00) — 56 اختباراً Flutter؛ أمن: **إصلاح XSS حقيقي** في print_manager (html.escape لكل مدخلات HTML — 4 اختبارات TDD) + تحقق مخطط URL في الإعدادات (رفض file:// — SSRF) + تدقيق SQL (كل القيم معاملات ?) + صفر أسرار صلبة + Rate-limiting مسجّل كمتطلب للنسخة السحابية → **2043 اختباراً أخضر** + PROJECT_MAP (جلسة 94)
- **جلسة إصدار v3.2.0 (2026-08-13):** رفع الإصدار عبر config.py + i18n ×3 + installer.iss + build_nuitka.py + pyproject → 3.2.0 + docs/version.json (mobile_url + changelog) + index.html (بطاقة تحميل الجوال dl_mob_* + سجل v3.2.0) + script.js ×3 لغات + README/CHANGELOG + بناء Nuitka + Setup 63.2MB + ZIP 103.3MB + APK 18.2MB + **GitHub release v3.2.0** — درس: رفع الإصدارات بـ Python io (لا PowerShell replace — يفسد العربية)
- **جلسة تثبيت هاتف حقيقي + إصلاحات الجوال (2026-08-13):** مستخدم ثبّت APK على هاتفه (عبر Gmail بإعادة تسمية `.bin` ثم Drive/خادم محلي) — إصلاح فشل تحميل snapshot من Gmail (content-URI) عبر `FilePicker.pickFiles(withData: true)` + utf8.decode + جولدن حتمي بحقن ساعة ثابتة في TaxCalendarScreen + **خادم تحميل محلي** `PhoneShare` (http.server 8080 + index.html عربي على `http://192.168.1.5:8080`) → **تحقق ناجح على هاتف المستخدم** + PROJECT_MAP (جلسة 92)
- **جلسة تطبيق الجوال (2026-08-12):** طلب «تطبيق جوال» — Flutter 3.44.7 في `mobile/` (رفيق متابعة Android: 3 لغات RTL + 5 شاشات + فك تشفير SACF1 متوافق مع سطح المكتب + تنبيهات جبائية + Health Score مطابق للـ desktop engine) — **43 اختباراً + analyze نظيف + APK arm64 18MB** — بيئة: Flutter في `C:\Users\khalile\flutter` + JDK 17 في `C:\Users\khalile\android\jdk17` + SDK في `C:\Users\khalile\android\sdk` + درس: AGP 9 يفشل مع file_picker/flutter_local_notifications → AGP 8.9.1 + Gradle 8.11.1 + Kotlin 2.1.20 + desugaring + `mobile/PROJECT_MAP.md` + مولد `tools/gen_mobile_fixtures.py` + PROJECT_MAP (جلسة 90)
- **جلسة منصة ذكاء اصطناعي متكاملة (2026-08-07):** طلب المستخدم «منصة ذكاء اصطناعي متكاملة» — محرك `modules/ai_platform.py` (Health Score 0-100 من 6 محاور + Risk Radar + ملخص تنفيذي + توصيات استراتيجية) + شاشة 39 `ai_platform_view.py` (بطاقة درجة + أشرطة توزيع + رادار عمودي + توصيات ملونة + روابط سريعة) + تسجيل في main_window (ribbon nav_group_tools + Ctrl+Shift+H) + i18n **2104** (+26 ×3) + 8 اختبارات → **2037 اختباراً أخضر** + PROJECT_MAP (جلسة 88)
- **جلسة تقارير IAS/IFRS (2026-08-07):** طلب المستخدم «تقارير مالية معيارية IAS/IFRS» — محرك `modules/ias_reports.py` جديد (4 تقارير: المركز المالي IAS 1 + الدخل الشامل IAS 1 + التدفقات النقدية IAS 7 + التغيرات في حقوق الملكية IAS 1) + شاشة 38 `ias_reports_view.py` (QTabWidget 4 تبويبات + تصدير PDF/Excel) + تسجيل في main_window (ribbon nav_group_analysis + Ctrl+Shift+G) + i18n **2078** (+64 ×3) + 6 اختبارات → **2029 اختباراً أخضر** + PROJECT_MAP (جلسة 87)
- **جلسة تفعيل ML كاملاً (2026-08-07):** طلب المستخدم «تفعيل ML في المشروع كاملاً دون إزالة بوابات AI» — إزالة استدعاءات `feature_allowed("ai_unlimited")` من `ai_insights_view.py` (3 مواضع: `_enforce_gates` 6 أشهر دائماً + `_export_pdf` + `_export_excel` مفتوحان للجميع) — محرك `modules/ai_insights.py` + تعريف البوابة في `tier.py` لم يُمسّا + بوابة `cloud_sync` بقيت على PRO + تحديث 3 اختبارات `test_entitlement.py` → **2023 اختباراً أخضر** + PROJECT_MAP (جلسة 86)
- **جلسة شريط Ribbon + إصلاح انهيار matplotlib + إعادة تشكيل 3 شاشات محاسبية (2026-08-07):** بعد عرض المستخدم 3 صور (عملاء/فواتير/مخزون) واشتكاء «متداخلين وعنوان خانات مش فوقها» — (1) **شريط ribbon** في main_window (QTabBar 6 تبويبات + لوحات أزرار الـ37 شاشة تحتها في صفين، قابل للطي ▲/▼) + أنماط #ribbonTabs/#ribbonBtn بالثيمات الثلاثة + مسح التبويبات عند إعادة البناء + حذف `_on_ribbon_tab_changed` الميتة؛ (2) **إصلاح انهيار حقيقي 0xC0000409**: `ThemeColors.get()` كانت تُرجع `rgba(255,255,255,0.10)` (dark/modern) فلا يقرؤها matplotlib → سقوط التطبيق عند رسم لوحة التحكم ببيانات — الحل تحويل rgba→#rrggbb داخل get() (درس: ألوان الرسوم matplotlib-compatible لا CSS)؛ (3) **إعادة تشكيل الشاشات الثلاث** بعناوين فوق الحقول عبر مساعد مشترك `BaseView._labeled_field()` (DRY) + زر الإضافة في نفس الصف بمحاذاة Qt.AlignBottom + inventory_view: إضافة عنصر في صفّين (8 حقول) + إزالة addStretch من الإحصائيات (تمتد بعرض كامل) + i18n **2014** (+inventory_price/inventory_min_qty ×3) + **3 اختبارات بنية** `test_form_fields_have_labels` (TDD: فشلت ثم نجحت) → **2023 اختباراً أخضر** + PROJECT_MAP (جلسة 85)
- **جلسة إصلاحات UI حسب صور المستخدم (2026-08-05):** بعد عرض المستخدم صوراً لخمس شاشات واشتكاء «الخانات صغيرة ومتداخلة ومش باينة» — (1) **اختبار المستخدمين**: بطاقتا الجلسات والملاحظات كانتا QGridLayout بأربعة/ستة أعمدة بلا معاملات تمدد → ضيق وتداخل في RTL → `setColumnStretch(1/3/5, 3)` لأعمدة الحقول + `name_edit` ناقص setMinimumHeight(40) — النتيجة: حقول الجلسات 497px والملاحظات 320px بارتفاع 40px؛ (2) **التقويم الجبائي**: upcoming_table كان Stretch متساوياً على 7 أعمدة فتُضغط الأعمدة → Interactive + عمود الاسم Stretch + عروض ثابتة (90/110/80/100/130/120)؛ (3) **التخطيط والمتابعة (budget_view)**: فراغ كبير في النصف — الجدولان بلا minimumHeight فيتمددان عمودياً → `verticalHeader 44` + `minimumHeight(44*6+30)` + results_table `QSizePolicy.Preferred`؛ (4) **المزامنة السحابية**: فراغ هائل في الأسفل — dest_table/backup_table بلا ارتفاع + addStretch() في النهاية → minimumHeight (44*3+30) لكل جدول + إزالة addStretch — **QA**: الشاشات الأربع تُبنى بعرض 1280×800 + حقول 40px + **2020 اختباراً أخضر** + PROJECT_MAP (جلسة 83)
- **جلسة توحيد التباعد — المرحلة 2 (2026-08-05):** بعد طلب المستخدم «كمل باقي شاشات» — فحصا `setContentsMargins` و`setSpacing` لكل الشاشات (باستثناء login_view المقصود) — بطاقات `objectName="card"` → **CARD_MARGINS (16,12,16,12)** في 10 شاشات (dashboard/ratios/analysis/advanced_dashboard 4/scenarios 3/benchmarks/ai_insights/cost_center_profitability/audit) + مجموعات QGroupBox الصريحة → **(15,20,15,15)** (settings 7 + zscore) + حذف آخر `setSpacing(18)` (settings ai/email→15) + حقول الإقرارات الستة `decl_*` في tax_view → 40px + Undo/Redo في data_entry (36→40) + `TableFilterWidget.search_input` المشتركة (32→40) — **QA**: Geometry check (قياس `minimumHeight` لا sizeHint الافتراضي 25px) = 0 حقول <38px + **2020 اختباراً أخضر** — **دروس**: استبدال جماعي PowerShell يكسر أسماء متغيرات layouts (استُعيد من git وأُعيد تعديل كل موضع باسم متغيّره) + `Set-Content -Encoding UTF8` يضيف BOM يكسر `ast.parse` في الاختبارات (أُزيل بالبايتات) + PROJECT_MAP (جلسة 82)
- **جلسة توحيد ارتفاعات العناصر (2026-08-05):** بعد عرض المستخدم صوراً لعدة شاشات أظهرت تبايناً في الحقول/الأزرار — **Audit AST شامل** للـ35 شاشة: 32 موضعاً بـ 36px بدل MIN_HEIGHT_FIELD=40 + أزرار 38/42/45/48/50 + هامشان منحرفان (zscore 25px/settings 40px) + 3 جداول بصفوف غير 44 — **الإصلاح**: كل الحقول والأزرار → **40px** في 12 شاشة (settings 17 موضعاً/advanced_dashboard 3/ai_insights/benchmarks/tax_calendar/user_testing 10/cost_center_profitability/zscore/chat/cashflow/tax/breakeven/budget/cost_center/dashboard/forecasting/scenarios) + هامشان → (20,20,20,20) + spacing 30→15 + صفوف الجداول الثلاثة → **44px** (تطابق `_normalize_table_rows`) + **لا تغيير** لـ login_view (تصميم مستقل مقصود) — **QA**: Geometry check برمجي (0 حقول <38px + الـ35 تُحمَّل) + **2020 اختباراً أخضر** + PROJECT_MAP (جلسة 81)
- **جلسة ميزة «تذكرني» — حفظ الجلسة المشفّر (2026-08-05):** بعد ملاحظة المستخدم أن التطبيق يطلب الدخول كل إقلاع — خانة `login_remember_me` في شاشتي الدخول والتسجيل (حالة التسجيل تنتقل للدخول عند العودة) — الجلسة في `ui/login_session.py` **مشفّرة AES-256-GCM بمفتاح SHA-256 لبصمة العتاد** (hardware_id.fingerprint): كلمة السر لا تُكتب نصاً أبداً + تعمل على جهاز التخزين فقط (جهاز آخر → بريد فقط وفشل آمن) + القديم (بريد فقط) مدعوم + KDF أخف 16MiB/1 للإقلاع + auto-login في MainWindow (QTimer) بحراسة: لا تلقائي عند must_change_password/2FA/كلمة خاطئة + logout يمسح كلمة السر ويبقي البريد؛ i18n **2012** (+1 ×3)؛ دروس: `insertWidget` يزيح فهرس QStackedWidget (المساواة مع مسار الدخول اليدوي لا رقم ثابت) + mock على `ui.views.login_view.save_login_session` (استيراد مباشر)؛ **29 اختباراً جديداً** → **2020 اختباراً + تغطية modules 100% + login_session 100%** + PROJECT_MAP (جلسة 80)
- **جلسة Module 2 + 3 التجاريان — التشفير والبوابات (2026-08-05):** بعد موافقة صاحب المشروع على النطاق «Module 2 + 3» (النشر الرقمي مؤجل لحين الحسابات الرسمية) — **Module 2** `commercial/encryption/`: Argon2id من `cryptography` المدمج (صفر تبعيات) + تنسيق ملفات ذاتي الوصف `SACF1` (AES-256-GCM، الـ tag يصادق الرأس كاملاً → أي تلاعب يفشل) + **دمج في cloud_sync**: `encrypt_payload` → Argon2id والقديم PBKDF2 يُقرأ تلقائياً (سنابات قديمة تبقى تعمل)؛ تأجيل مقصود: تشفير accounting_data.json مؤجل (يكسر التصدير/النسخ/المزامنة)؛ **Module 3** `commercial/entitlement.py`: `current_tier()/feature_allowed()/set_store()/reset()` — **بوابات مفعّلة فعلياً**: cloud_sync=PRO على push/pull (النسخ المحلي حر) + ai_unlimited=ENTERPRISE (إزالة 6 أشهر + منع تصدير AI، FREE تحتفظ بـ3 أشهر) + رسالة مرفوضة مترجمة مع تلميح الترخيص؛ i18n **2011** (+4 ×3)؛ دروس: QWidget بلا `QApplication(sys.argv)` يهشم pytest + `QMessageBox.warning` غير المعمّى يحجب 116s؛ **47 اختباراً جديداً** → **1991 اختباراً + تغطية modules 100% + commercial 99%** + docs/COMMERCIAL_MODULE2_3_SUMMARY.md + PROJECT_MAP (جلسة 79)
- **جلسة Module 1 التجاري — الترخيص والتفعيل (2026-08-05):** بعد موافقة صاحب المشروع على Prompt v4.0 Commercial Build (القرارات: Module 1 الآن + **PyInstaller** بدل Nuitka لاحقاً + تأجيل النشر لحين الحسابات الرسمية) — `commercial/licensing/` كاملة: مفاتيح RSA-2048 بتوقيع PKCS1v15 SHA-256 (تنسيق `base64(payload)---base64(signature)` بخانات 5 أحرف — تنسيق XXXX البشري في الـ prompt مستحيل فيزيائياً لتوقيع 256 بايت؛ موثّق) + تثبيت على عتاد المستخدم (MAC+CPU+disk → SHA-256 مع fallbacks) + انتهاء مع مهلة 14 يوماً ثم **read-only** + طبقات FREE/PRO/ENTERPRISE ببوابات `feature_enabled()` (لا قفل لأي ميزة بعد) + `LicenseStore` (challenge + حفظ ذرّي license.dat) + CLI `keygen` للبائع فقط + 5 مفاتيح تجريبية مقابل HWID وهمي + حوار التفعيل في قائمة Help + تنبيه الإقلاع فقط عند is_read_only + i18n **2007** (+21 ×3) — **درس Qt/pytest**: QDialog لا يُبنى تحت pytest على هذا الجهاز (QWidget يعمل) → منطق الحوار في دوال نقية `describe_license/try_activate` والفئة `# pragma: no cover`؛ **67 اختباراً + تغطية 99%** على الحزمة + **1944 اختباراً كاملاً + تغطية 100%** + docs/COMMERCIAL_MODULE1_SUMMARY.md + PROJECT_MAP (جلسة 78) + تحقق افتراضات الـ prompt (api_key فارغ أصلاً/cloud_sync محلي سليم/لا LICENSE في الجذر) — **المفتاح الخاص `commercial/keys/` + license.dat لا يُرفعان أبداً (gitignored)**
- **جلسة المرحلة 1 من التنظيم الشامل للواجهة (2026-08-05):** بعد موافقة صاحب المشروع على النطاق «المرحلة 1 الآمنة أولاً» من Prompt «Complete Application UI/UX Organization & Design» (المرحلتان 2-3 — Header علوي + Sidebar مجمّع — مؤجلتان عمداً لئلا تكسر مفاتيح i18n sidebar_* ومصانع المشاهد المرقّمة 1-35): (1) **نافذة متكيفة**: main_window.py يستبدل `setGeometry(100,100,1440,880)` بـ `QApplication.primaryScreen().availableGeometry()` + حد أدنى متكيف `min(1200, العرض)×min(800, الارتفاع)` — تفتح بحجم شاشة المستخدم (1920×1080 أو دقته) ولا تتجاوز الشاشات الأصغر؛ (2) **شريط حالة 40px** في الثيمات الثلاثة (min-height + `QStatusBar::item{border:none}` + `QStatusBar QLabel{color}`) + **ملصق إصدار دائم** `QLabel#versionLabel = v{APP_VERSION}` (استيراد APP_VERSION نُقل لرأس الملف — كان داخل دالة)؛ (3) **صفوف الجداول 44px**: تحقق أن `min-height` QSS لا يعمل على `QTableWidget::item` (بقيت 31px) → `MainWindow._normalize_table_rows()` برمجياً عند الإنشاء الكسول لكل المشاهد (بلا لمّس الشاشات)؛ (4) **QTabWidget/QTabBar** أُضيف للفاتح (كان بلا قسم) + **QScrollBar:horizontal** للفاتح (كان عمودياً فقط)؛ (5) **QA**: 260 واجهة + **1877 اختباراً + تغطية 100%** + تحقق برمجي (نافذة/شريط/إصدار/صفوف) + docs/UI_STYLE_GUIDE.md (قسم 6) + PROJECT_MAP (جلسة 77)
- **جلسة التوحيد الشامل لتباعد الواجهات (2026-08-05):** `ui/constants.py` (المصدر الموحد: SPACING 5/8/10/15/20 + MARGIN 5/10/15/20 + MIN_HEIGHT 40 + PAGE/CARD/STAT/FORM + `apply_standard_layout`) + `ui/views/_base.py` على المعايير (17 شاشة ترث تلقائياً) + توحيد التباعد 3-5→8 في 9 شاشات (audit/advanced_dashboard/dashboard/analysis/scenarios/tax_calendar/tax ×3/chat + login مقصودة 5) + **الثيمات الثلاثة**: اكتشاف نموذج صندوق Qt (min-height يُضاف فوق padding — كانت أزرار 62px) → padding رأسي 0 + min-height 40 = **حقول 42-44px/أزرار 42px موحّدة** + 15 اختبار `test_ui_constants.py` + Geometry QA للشاشات الـ35 + لقطات BEFORE/AFTER بمقارنة pixel-diff (12 شاشة تغيّرت) → **1877 اختبار + تغطية 100%** + docs/UI_STYLE_GUIDE.md (المرجع الإلزامي) + docs/UI_SPACING_AUDIT_REPORT.md
- **جلسة رفع الإصدار v3.1.8 (2026-08-04):** config.py + i18n window_title ×3 + installer.iss + build_nuitka.py + pyproject.toml → 3.1.8 + docs/version.json (changelog v3.1.8 + URLs) + docs/index.html (badge/softwareVersion/روابط تحميل/سجل v3.1.8 + 1862 اختباراً) + docs/script.js (hero_badge + upd_date9 + upd_new34/35 ×3 لغات) + README badges + KNOWLEDGE_BASE/USER_GUIDE/API_REFERENCE/openapi.yaml/CHANGELOG (v3.1.8 أعلى السجل) + PROJECT_MAP + AGENTS.md + إعادة بناء Nuitka + Inno Setup + ZIP محمول كـ v3.1.8 + GitHub release v3.1.8 بأحدث assets + auto-update يشير إلى v3.1.8
- **جلسة التحسينات الأساسية (2026-08-05):** بعد موافقة صاحب المشروع على خطة التنظيف — (1) **تنظيف 105 استيرادات غير مستخدمة** عبر 40 ملفاً (أداة AST `clean_imports2.py` + إزالة shutil الزائد) → **1862 اختبار + تغطية 100%**؛ (2) **إعادة بناء المثبّتات** لتطابق الريبو (Setup 63.8MB / ZIP 104.3MB)؛ (3) **اختبار ترقية حقيقي v3.1.7→v3.1.8** (installer /DIR=): ملفات الحالة **متطابقة بايت-بايت** بعد الترقية (SHA-256) + دخول مستخدم سابق نجح على v3.1.8 — اثبات Excludes يعمل؛ (4) **مزامنة وثائق حية**: script.js hero_badge EN/FR → v3.1.8 (العربية كانت محدّثة بالفعل) + QUALITY_POLICY/DEVELOPMENT_METHODOLOGY → v3.0.0-3.1.8
- **جلسة إصدارات النظام الجبائي السنوية (2026-08-04):** تحليل نشرة DGI الرسمية «النظام-الجبائي-الجزائري-2026.pdf» (43 صفحة) → `modules/tax_years.py` (إدارة سنوات: list/load/save/copy/delete/validate/import-export + تجلد `modules/config_years/` + مؤشر `.active_year`) + ملفّا `tax_config_2025.json` (مرجعي) و`tax_config_2026.json` (وفق النشرة) + توسيع TaxEngine (set_year/list_years + 3 حاسبات جديدة) + تبويب «سنوات النظام» في TaxView (مبدّل سنوات + محرر JSON بتحقق/حفظ/استيراد/تصدير + حاسبات) + تصحيح شرائح IRG 2026 (0/23/27/30/33/35% بحدود 240k/480k/960k/1920k/3840k) + إضافة IFU (0.5/5/12% حد أدنى 30,000) + رسم التكوين والتمهين (1%+1% خصم بالإنفاق) + اقتطاع الإيجارات (7%/15%/مؤقت 7% فوق 1.8M) + **دمج عميق** مع الافتراضية للملفات القديمة + i18n 1986 (+61 ×3) → **1862 اختباراً** + `docs/TAX_SYSTEM_YEARS.md`
- **جلسة طلب براءة اختراع INAPI (2026-08-04):** `thesis/PATENT_APPLICATION.md` — ملف براءة اختراع كامل بالفرنسية (لغة INAPI) — العنوان: Système et procédé automatisé d'analyse financière conforme à la fiscalité algérienne intégrant IA pour PME — 10 قسم: domain technique + état de l'art (G1-G5) + exposé inventif + description détaillée (5 figures) + 10 revendications (6 نظام + 2 إجراء + 1 دعم + 1 واجهة) + résumé + dessins (10 figures) + données prototype + novéauté + appel de protection — المحميات: TaxEngine (IBS/TVA/IRG/CNAS/CNAC/VF) + 20 ratios + Z-Score + DuPont + AIInsights (3 forecasting + anomalies) + Benchmark (7 sectors) + Scenarios + Security (PBKDF2/AES/2FA) + CloudSync (AES-GCM) + i18n (1925×3) + lazy loading (44ms) — الطالب: MBKAREK Ahmed Khalil، المحاسبة والجباية، ابن خلدون تيارت
- **جلسة استكمال ملف الجودة ISO (2026-08-04):** `docs/DEVELOPMENT_METHODOLOGY.md` — توثيق منهجية التطوير الفعلية (بند ISO 9001 8.1/8.3/10.2): دورة حياة الميزة (تحليل تأثير ← تصميم DRY ← تعديل جراحي ← TDD ← مجموعة كاملة ← توثيق ← مزامنة الحالة ← Conventional Commit) + الأدوار + معايير القبول Definition of Done (1800/1800 + تغطية 100% + لا أسرار + توثيق محدّث) + إجراءات عدم المطابقة (RCA + اختبار انحدار) + إدارة التكوين والإصدارات + traceability — رُبطت في KNOWLEDGE_BASE.md + QUALITY_POLICY.md — **CI على GitHub أخضر (1800 passed, 27.8s)** — ملف الجودة الآن مكتمل للتدقيق (QUALITY_POLICY + DEVELOPMENT_METHODOLOGY + CHANGELOG + PROJECT_MAP EXECUTION LOG + CI)
- **جلسة مراجعة شاملة + جاهزية ISO 9001 (2026-08-04):** مراجعة المشروع/الفيديو/التوثيق — (1) **إصلاح اختبار فاشل**: `pypdf` ناقص في requirements-dev.txt → أُضيف `pypdf>=4.0,<6.0` (1800/1800 ناجح)؛ (2) **CI أحمر**: كان يثبّت `requirements.txt` فقط → أصبح `pip install -r requirements-dev.txt`؛ (3) **توحيد الإصدارات**: pyproject.toml 2.1.0→3.1.7 + README badge 1.0.0→3.1.7 + README محتوى محدّث (35 شاشة/20 نسبة/بنية/اختبارات)؛ (4) **توثيق 29→35 شاشة**: USER_GUIDE (شاشات 30-35 + قسم 2.7 + صفوف الجدول) + KNOWLEDGE_BASE + index.html meta 27→35؛ (5) **وثائق ISO**: `docs/QUALITY_POLICY.md` (بيان سياسة + 9 أهداف قابلة للقياس + مخاطر ISO 9001) + `CHANGELOG.md` (v3.0.0→v3.1.7 + traceability note لـ v3.1.5)؛ (6) **تنظيف 274MB**: zips v3.1.1/3.1.2 + cloudflared.exe + Setup-v3.0.0.exe من docs/؛ (7) **وسوم git**: جُلب v3.0.0/3.1.3/3.1.4/3.1.6/3.1.7 — v3.1.5 بلا commit مستقل (دُمج في a2e6cd2) → موثّق في CHANGELOG
- **تحسين الفيديو حسب ملاحظات المستخدم (2026-08-04):** العربية كانت بلا تشكيل واتجاه خاطئ → `arabic_reshaper` + `python-bidi` في generate_slides.py (غلاف `_SmartDraw` يعيد تشكيل أي نص عربي تلقائياً: إنجليزي LTR / عربي RTL متصل) + إعادة توليد 11 شريحة + موسيقى خلفية مولّدة (أكورد Cmaj7 + ألحان + fade in/out) عبر `AudioArrayClip` → MP4 (76s/1080p/24fps/AAC)
- **جلسة فيديو تعليمي (2026-08-04):** `docs/VIDEO_TUTORIAL.md` (سكربت 12-15 دقيقة / 9 فصول: افتتاحي + دخول + إدخال بيانات + لوحة تحكم + تحليل متقدم + ضريبة + ذكاء + إنتاجية + ختامي + VO بالإنجليزية + overlays + إرشادات إنتاج) + `docs/VIDEO_STORYBOARD.md` (shot list تفصيلي 11 مشهداً / overlay style guide / موسيقى) + `thesis/generate_slides.py` → 11 شريحة PNG 1920×1080 (Pillow + Amiri Arabic) في `thesis/video_slides/` — يغطي كل الشاشات الـ35 + اختصارات Ctrl+1-0/F2-F12 + النظام الجبائي الكامل (IBS/TVA/IRG/CNAS/CNAC/VF + التقويم + الإقرارات) + الرؤى الذكية (3 طرق تنبؤ + كشف شذوذ + توصيات) + المزامنة السحابية + التصدير (PDF/Excel)
- **جلسة حزمة مذكرة الماستر (2026-08-04):** `thesis/` — OUTLINE.md (I-X تفصيلي: 35 شاشة/20 نسبة/37 وحدة/1800 اختبار بدل الأرقام القديمة 22/318) + INTRO_DRAFT.md (مقدمة ~3 صفحات بحقائق موثّقة من بحث: 1.36M PME نهاية 2022، 95% من المنشآت، >70% اليد العاملة، كثافة 28/1000 + معدلات ضريبية فعلية من tax_config.json) + LITERATURE_REVIEW_FRAMEWORK.md (استراتيجية بحث + مصفوفات + فجوات G1-G5) + STYLE_GUIDE.md + REFERENCES_TEMPLATE.md (APA 7 + نصوص تنظيمية) + generate_template.py → thesis_template.docx (غلاف/دروس/ملخصات 3 لغات/TOC حقل/ترقيم روماني-عربي/8 فصول/ببليوغرافيا/ملاحق، A4 TNR 12 سطر 1.5) — «Law 1275» = **القرار الوزاري 1275 (27/09/2022) «شهادة ↔ مؤسسة ناشئة/براءة اختراع»**؛ الأرقام الصحيحة: 1800 اختبار/35 شاشة/1925 مفتاح i18n ×3
- **جلسة معالجة ملاحظات تحليل المشروع (2026-08-04):** الملاحظات السبع من تحليل المشروع: (1) **تلوّث git** — `git rm --cached` لـ `.vault_salt` + `accounting_data.json.bak` + `activity_log.json` + `nuitka-crash-report.xml` (−32,652 سطراً) + توسيع .gitignore (chat_history.json/login_session.json/*.db-shm/*.db-wal/artifacts/)؛ (2) **مخلفات الجذر** — سجلات Nuitka والاختبارات نُقلت إلى `artifacts/` وحُذفت tmp*/file.txt/tunnel_*؛ (3) **ترويسة PROJECT_MAP** — قسم «آخر حالة» + EXECUTION LOG جديد برموز ✅/🔶/⛔؛ (4) **اختبارات أداء** `tests/test_startup_perf.py` (4: إقلاع 44ms/مشاهد 95-402ms/ذاكرة ≤45MB — بحدود متسامحة تتحمّل الأجهزة البطيئة)؛ (5) **CI** `.github/workflows/ci.yml` (ubuntu + Python 3.11 + libegl1/libgl1/libxkbcommon0/libdbus-1-3/xvfb + `xvfb-run pytest tests -q` — تغطية معلوماتية فقط دون `--fail-under=100`)؛ (6) **تفكيك setup_ui الست العملاقة** إلى دوال `_build_*` (data_entry 350→27/settings 261→15/scenarios 224→28/analysis 217→31/benchmarks 212→24/user_testing 192→9 — إعادة ترتيب صرفة بلا تغيير منطقي + تحقق 245 passed لكل ملف)؛ (7) **طبقة التصدير الموحدة** `ui/exporters.py` (new_workbook/add_excel_sheet/ask_save_path/style_header_row/write_charts_pdf) + ترحيل 3 شاشات (advanced_dashboard/ai_insights/cost_center_profitability −180 سطر boilerplate بمخرجات مطابقة) + 9 اختبارات `test_exporters.py` → **1800 اختباراً**
- **جلسة إصلاح مشاكل الـ v3.1.7 الخمس المتبقية (2026-08-03):** (1) **شاشات فارغة في التثبيت الفعلي** — الجذر الحقيقي: الترقية القادمة من Setup-v3.1.6 وصلت ناقصة الملفات الحرجة (319 ملفاً: لا sqlite3.dll/_sqlite3.pyd ولا pandas/_libs ولا pytz/zoneinfo) بينما Setup-v3.1.7 كامل (985 ملفاً) — نُسخت الملفات المفقودة + تحقق UI Automation أن dupont/benchmarks/advanced_dashboard/cloud_sync/user_testing/ledger/inventory/payroll/budgeting تُحمَّل كلها بلا exceptions.log؛ (2) **DuPont (4) يتجمّد عند العودة** — سببه ملفات numpy/matplotlib الناقصة في التثبيت — بعد النسخ تحقّق آلي بدورة عودة مزدوجة بلا تجمّد (~0.6s للنقرة)؛ (3) **أرقام تظهر قبل الإدخال** — `data_entry.py::_make_spin()` كان يعرض `0.00` — أصبح `setSpecialValueText(t("de_enter_amount"))` (مفتاح i18n جديد ×3 = 1925 مفتاحاً) + اختبار انحدار `test_financial_spins_show_empty_before_input`؛ (4) **التحديث يطلب سرّية جديدة** — الجذر: `installer.iss` كان يضمّن users.json (must_change_password) + settings.json + accounting_data.json + DB عند الترقية فتُستبدل بيانات المستخدم؛ الإصلاح — `Excludes` في [Files] تستثني ملفات الحالة كلها (users/settings/accounting_data/chat_history/login_session/activity_log + accounting_platform.db + templates\custom_templates.json + logs/data/backups) — **تحقّق حقيقي**: تثبيت نظيف صامت (975 ملفاً بلا ملفات حالة) + أول تشغيل يُنشئها ودخول admin نجح + **ترقية فوق تثبيت سابق حافظت على users.json الحرفي**؛ (5) إعادة بناء Nuitka (exe **143MB**) + Inno Setup (**66.9MB**) + ZIP محمول (**109.4MB**، 973 ملفاً) → **1787 اختباراً**
- **جلسة إصلاح «الشاشات الفارغة» في المثبّتات (2026-08-03):** مستخدم أبلغ شاشات فارغة في النسخة المثبّتة — **الجذر**: التحميل الكسول يستورد المشاهد ديناميكياً (`_lazy_view_factory` main_window.py:29 + PEP 562 في modules/__init__.py) وNuitka لا يضمّن الاستيرادات الديناميكية → `ui/views/*` وكل محرّكات modules كانت **غائبة من الـ exe** (سجل المثبّت: دخول ناجح ثم صمت، لا سطر main_window — يبقى الـ placeholder فارغاً)؛ **الإصلاح**: `--include-package=ui.views,ui.resources,modules` في build_nuitka.py + build_nuitka.bat يستدعي build_nuitka.py (كان أمراً قديماً مكرراً) + إعادة بناء Nuitka (**143MB** بدل 82MB) + Inno Setup (**66.9MB**) + ZIP محمول (**109MB**) — التحقق الفعلي: دخول عبر UI Automation على exe محدّث → `Lazy-loaded view: data_entry` + dashboard/tax/audit/reports/chat/cost_center + مشاهد الـ ZIP بدون أخطاء + مصانع المشاهد الـ35 تنجح + **تحقق المستخدم النهائي عبر مسار التحديث الحقيقي**: تثبيت v3.1.6 (المصاب) ثم التحديث التلقائي إلى v3.1.7 → **«تم التحميل بنجاح والشاشات شغالة»**
- **جلسة مراجعة مواد الدفاع (2026-08-03):** `docs/generate_report.py` — v3.1.6→v3.1.7 (غلاف/ملخص/جلسات v3.1.3→v3.1.7) + 1350→**1786** اختبار (6 مواضع) + قسم **13.12 UAT الشامل (v3.1.7)** + تحديث أحجام المثبّتات (Setup 51.6MB/ZIP 82.9MB) + إعادة توليد التقرير: DOCX (52832B) عبر generate_report.py + PDF (257204B) عبر docx2pdf/Word COM — التحقق: v3.1.7 ×4 و1786 ×6 في DOCX
- **جلسة رفع الإصدار v3.1.7 (2026-08-03):** config.py + i18n window_title ×3 + installer.iss + build_nuitka.py → 3.1.7 + docs/version.json (changelog v3.1.7 + URLs) + docs/index.html (badge/softwareVersion/روابط/سجل v3.1.7) + docs/script.js (hero_badge + upd_date8 + upd_new31 ×3 لغات) + KNOWLEDGE_BASE.md/USER_GUIDE.md/openapi.yaml + AGENTS.md + إعادة بناء Nuitka (82MB) + Inno Setup (51.6MB) + ZIP محمول (83MB) + GitHub release v3.1.7 بأحدث assets + auto-update يشير إلى v3.1.7
- **جلسة UAT شامل (2026-08-02):** `tests/test_uat.py` الجديد (9 اختبارات) — رحلة مستخدم حقيقية عبر MainWindow (تسجيل دخول + تجوّل الشاشات الـ35 + إدخال بيانات وحساب + تبديل 3 لغات + save_to_db + تسجيل خروج) **كشفت بغّاً حقيقياً**: `bank_sync_view.py:238` و`data_import_view.py:260` يستدعيان `self._clear_layout()` في retranslate دون وجودها → AttributeError (سقوط التطبيق عند تبديل اللغة مع المشهد محمّلاً)؛ الإصلاح — `_clear_layout()` + `_clear_nested()` في `ui/views/_base.py` (BaseView) + 5 اختبارات انحدار (test_ui_views.py 114) → `_base.py` عند 100%
- **1800 اختباراً كلها ناجحة عبر `python -m pytest tests -q`** (1787 سابقة + 4 أداء في test_startup_perf.py + 9 تصدير في test_exporters.py — test_bank_print ضمن المجموعة غير الواجهة)
- **جلسة معالجة Smart App Control (2026-08-02):** سكربت `tools/allow_smart_app_control.ps1` جديد — يفحص حالة SAC من السجل (`HKLM:\...\CI\Policy\VerifiedAndReputablePolicyState`) ويعرض تعليمات الإيقاف بالعربية + يضيف استثناءات Defender للمثبّت (installer_output) وبنية Nuitka (dist_nuitka) + يفحص حالة توقيع الـ exe (Get-AuthenticodeSignature) — التحقق على جهاز التطوير: SAC مفعّل Enforcement + exe غير موقّع (NotSigned)؛ السكربت يحتاج UTF-8 BOM لأنه PowerShell 5.1 يقرأ cp1252 فيفشل مع العربية + تحديث التوثيق (USER_GUIDE.md جدول + سطر الملاحظة، docs/index.html dl_note_sac، docs/script.js بالثلاث لغات) لإرشاد المستخدمين للسكربت أو إيقاف SAC
- **إصلاح الشاشة السوداء عند التنقل (جلسة v3.1.6):** `_fade_in_view` في main_window.py كان يخزّن الأنيميشن في مرجع واحد `self._view_anim` فيُتلفف الأنيميشن السابق أثناء طيرانه فيبقى QGraphicsOpacityEffect عند شفافية ~0 دون إطلاق `finished` → شاشة سوداء دائمة على الأجهزة البطيئة؛ الإصلاح — حاوية `self._view_anims` + إيقاف الأنيميشن السابق + `QTimer.singleShot(300)` كضمانة تُزيل التأثير دائماً + حماية من finished متأخر (5 اختبارات واجهة جديدة: TestFadeInSafety)
- **جلسة تغطية الواجهات + i18n (2026-08-02):** `tests/test_ui_views.py` الجديد (111 اختباراً، 25 فئة) يغطي الشاشات الـ24 غير المغطاة سابقاً (dashboard/ratios/audit/reports/settings/chat/tax/comparative/cashflow/security/zscore/forecasting/budget/cost_center/breakeven/data_import/bank_sync/scenarios/advanced_dashboard/ai_insights/currency/cloud_sync/demo_data/user_testing/analysis) + **إصلاح بغّ حقيقي** `bank_sync_view.py:229,231` (استدعاء `ThemeColors.get()` بوسيطين — الدالة تقبل وسيطاً واحداً → TypeError عند رسم جدول المعاملات؛ أصبح بوسيط واحد) + **إصلاح تسرّب حالة** في test_ui_views.py (`test_calculate_cash_flow` + `test_add_year_requires_data` كانا يعدّلان `state.financial_data` دون استعادة → كسر `test_financial_spins_start_zero` في test_ui.py عند التشغيل المتسلسل) + **إضافة 44 مفتاح i18n مفقوداً** (chat_offline_* للمساعد المحلي + breakeven_chart_title + cloud_action/destination/status + cost_profit_prev_profit + login_reg_display) عبر اللغات الثلاث
- **جلسة تحسينات UI حسب ملاحظات المستخدم (2026-08-02):** 5 شاشات — لوحة التحكم المتقدمة (اللون المميز يُعرض باسمه المُترجم بدل رمزه `#2196F3` عبر مفاتيح color_* الجديدة) + ربحية المراكز (صفوف جدول مراكز التكلفة 46px + حد أدنى للارتفاع) + المزامنة السحابية (صفوف سجل العمليات 44px + حد أدنى) + الشركات التجريبية (صفوف المعاملات الشهرية 42px + حد أدنى) + اختبار المستخدمين (حقول 36px + خانة التعليق 110px بدل 70 + جدول الملاحظات 48px مع word-wrap) + **إصلاح عزل اختبارات** في test_ui.py (TestDataEntryView.setUp أصبح يستدعي `state.clear()` — كان تشغيل التطبيق وحفظ بيانات إلى accounting_data.json يكسر اختبارات «يبدأ فارغاً»)
- **1800 اختباراً كلها ناجحة عبر `python -m pytest tests -q`** (1787 سابقة + 4 أداء في test_startup_perf.py + 9 تصدير في test_exporters.py — test_bank_print ضمن المجموعة غير الواجهة)
- تغطية الوحدات: **100%** عبر `python -m coverage run --source=modules --omit="modules/__init__.py" -m pytest tests -q`
- **المرحلة الثانية — 6 شاشات محاسبية جديدة (شاشات 30-35):** محركات ledger.py + partners.py + invoicing.py + inventory.py + payroll.py + budgeting.py (ميزان مراجعة/تقادم الديون/TVA/متوسط تكلفة/CNAS+IRG/مقارنة الميزانية) + واجهات ledger_view/partners_view/invoicing_view/inventory_view/payroll_view/budgeting_view + ربط في main_window (factories 30-35 + sidebar 35 + اختصارات F10/F11/F12 + Ctrl+Shift+B/C/D) + إصلاح apply_language (sidebar_user_testing المفقودة كانت تحذف شاشة 29 عند تغيير اللغة)
- التوثيق الشامل (جلسة v3.1.6): API_REFERENCE.md أعيدت بصيغة UTF-8 + docs/api/openapi.yaml (OpenAPI 3.0 — 325 عملية، تُعرض في docs/api/index.html) + USER_GUIDE.md (29 شاشة/ممارسات/FAQ) + tutorials/ (4 سكربتات فيديو AI) + KNOWLEDGE_BASE.md (فهرس)
- 20 نسبة مالية في RatiosView + z_score في منفصل
- 3 حقول إدخال جديدة: cash, operating_expenses, average_payables
- شاشة DuPont متقدمة: شلال/خط/مؤشر ROE + مقارنة قطاعية + توصيات + تصدير PDF
- شاشة تحليل السيناريوهات: مثالي/طبيعي/أسوأ + حساسية Tornado + خط/شريط/مساحة + PDF + حفظ JSON/DB
- ميزة المعايير المرجعية المتقدمة: أفضل الممارسات + معيار دولي + نقاط قوة/ضعف + اتجاه عبر السنوات + مقارنة منافسين (جدولا reference_standards + competitor_data)
- لوحة التحكم المتقدمة: 6 بطاقات KPI بحالة لونية + 4 رسوم (إيرادات شهرية/ربعية + مصروفات + ربحية + رادار) + تنبيهات (شذوذ/أداء/معايير/إجراءات) + تخصيص كامل + حفظ تخطيطات DB (جدول dashboard_layouts) + تصدير PDF/Excel
- ميزة الامتثال الضريبي الجزائري: نسبة TVA 6% (intermediate) + ترحيل رصيد TVA + دفعات IBS المقدمة (أشهر 3,6,11) + تصفية IBS + بيانات DAS + قوالب إقرارات G50/G57/DAS مع تصدير PDF/Excel (modules/tax_reports.py + تبويب إقرارات في tax_view.py)
- محرك الرؤى الذكية AI (شاشة 24): تنبؤ 3-6 أشهر (خطي/متوسط متحرك/تجانس أسي + فترات ثقة 95%) + كشف شذوذ (z-score للأرباح + IQR للمعاملات) + أنماط (اتجاه/موسمية/دورات/مؤشرات مخاطر) + توصيات ذكية + تنبيهات (خطر/تحذير/فرصة/إجراء) + تصدير PDF/Excel (modules/ai_insights.py + ui/views/ai_insights_view.py — pandas/numpy فقط بدون sklearn)
- تحليل ربحية مراكز التكلفة (شاشة 25): مراكز (قسم/مشروع/فرع/خط إنتاج) + توزيع مباشر/غير مباشر (إيرادات/عدد موظفين/مساحة/متساوٍ) + تحليل ربحية + مقارنات (سابقة/ميزانية/معايير) + اتجاه متعدد الفترات + تقارير/توصيات + تصدير PDF/Excel (modules/cost_center_profitability.py + ui/views/cost_center_profitability_view.py)
- تحسين أداء شامل (v3.1.6): تحميل كسول للمشاهد (main_window.py) + PEP 562 للوحدات (modules/__init__.py) + تجمّع اتصالات DB (db_connection.py + close_pool) + دفعات executemany (db_operations.py) + لوحة تحكم بلا إعادة رسم (dashboard.py) — إقلاع 778→49ms، RSS 128→45MB، حفظ DB 4.6×، قراءة 17× (التفاصيل: docs/PERFORMANCE_REPORT.md)
- تعدد العملات (شاشة 26): 7 عملات افتراضية + أسعار صرف + تحويل + تقرير متعدد العملات + تصدير CSV (modules/currency.py + ui/views/currency_view.py + حالة في app_state.py)
- المزامنة السحابية والنسخ الاحتياطي (شاشة 27): وجهات مزامنة (Dropbox/OneDrive/Drive) + snapshot مع checksum + تشفير اختياري بكلمة مرور (AES-GCM) + نسخ احتياطي تلقائي مع تدوير + استرجاع/سحب + سجل عمليات DB (modules/cloud_sync.py + ui/views/cloud_sync_view.py — جدول cloud_sync_state)
- الشركات التجريبية (شاشة 28): 4 شركات (تجارية/خدمات/إنتاج/استيراد-تصدير) ببيانات مالية وضريبية متسقة + مولّد معاملات شهرية (12 شهراً بأوزان موسمية) + تحميل شركة للتطبيق + تقارير مُعدّة مسبقاً (ميزانية/دخل/نسب) + تصدير بيانات CSV + قوالب استيراد CSV متوافقة مع DataImporter (modules/demo_data.py + modules/demo_templates.py + ui/views/demo_data_view.py)
- تحسينات UI/UX (شاشات 28): اختصارات كاملة (Ctrl+1..0، Ctrl+Shift+1..0، Ctrl+Shift+A، F2..F8 للشاشات 21-28، Ctrl+T للثيم، F1 للاختصارات) + قائمة "عرض" ديناميكية بكل الشاشات 28 + نافذة اختصارات كاملة + انتقالات تلاشي عند تغيير الشاشة + مؤشر تحميل في شريط الحالة + رسائل خطأ موحّدة مع إجراء مقترح مترجم (ui/widgets/messages.py + ربط أخطاء الطباعة/التصدير/الاختبارات) + تحسينات إتاحة/تباين بالثيمات الثلاثة (focus/disabled/ComboBox/ToolTip + تباين نص داكن)
- شاشة اختبار المستخدمين (شاشة 29): 4 مجموعات مستخدمين × 5 سيناريوهات + ملاحظات/مقترحات/أعطال بتصنيفات/أولويات/حالات + درجة رضا بقيود + تحليل مفصّل + تقارير (تعقيبات/قائمة أعطال/طلبات تحسين/ملخص) + بيانات تجريبية + تصدير/استيراد JSON + CSV/Excel/PDF + حفظ/تحميل DB (modules/user_testing.py + ui/views/user_testing_view.py — قنوات DB عبر get_connection)
- 1350 اختباراً كلها ناجحة عبر `python -m pytest tests -q` (1127 عبر `python -m unittest discover -s tests`)
- تغطية الوحدات: **100%** عبر `python -m coverage run --source=modules --omit="modules/__init__.py" -m pytest tests -q` (5768 سطراً، 0 مفقود — كانت 73% ثم 99%)
- التغطية الشاملة: 20 ملف اختبار جديد (test_edge_errors, test_cashflow, test_comparative, test_backup, test_bank_print, test_breakeven_costcenter, test_reporting_modules, test_email_currency, test_importers, test_excel_export, test_reporting_extra, test_user_manager, test_update_checker_extra, test_cloud_sync_extra, test_tax_reminders_extra, test_tax_reports_extra, test_small_gaps) — الميزات الست (DuPont/Scenarios/Benchmarks/AI/Tax/Anomaly) + cashflow + comparative عند 100% + قائمة أخطاء مفصّلة في PROJECT_MAP.md
- اختبار التكامل (جلسة v3.1.6): 3 ملفات جديدة — test_integration_workflow.py (9: رحلات مستخدم كاملة + إدارة حالة + اتساق تدفق البيانات) + test_integration_database.py (18: سلامة/معاملات/تزامن/نسخ-استرجاع) + test_integration_performance.py (10: 1000+ معاملة + مستخدمون متزامنون + بيانات كبيرة + إجهاد ذاكرة) — كشفت 3 أخطاء منتج أُصلحت (working_capital لا يُخزَّن في analysis.py، delete_analysis يفشل مع notes في db_operations.py، backup.py يفقد بيانات WAL) + تقرير docs/INTEGRATION_REPORT.md
- شاشة اختبار المستخدمين (جلسة v3.1.6): test_user_testing.py (66: جلسات/ملاحظات/رضا/تقارير/JSON/DB/أعطال تصدير) — user_testing.py وصل 100% + إصلاح تصدير PDF عند غياب خط عربي (cp1252 fallback)
- جلسة التصحيحات النهائية (2026-08-01): إصلاح 13 خللاً (print_manager/landscape، bank_sync رأس الملف، report_templates deepcopy، reporting Amiri، update_checker download+fallback، user_manager token=None، scheduled_backup vault.enc+meta، backup SQL/تحقق sqlite، data_import disconnect+محجوزات، currency no-op، tax_reminders فرع ميت، i18n v3.1.6) + مراجعة أمان (PBKDF2 100k/salt، تخزين مشفّر SMTP/API، HTTPS فقط — كلها سليمة) → **تغطية 100%**
- إصلاح واحد ضمن جلسة التغطية: `modules/comparative.py generate_report` كان يرمي KeyError مع بيانات ناقصة → `.get(item/ratio, 0)`
- i18n: 1924 مفتاحاً × 3 لغات (AR/EN/FR) — مجموعات متطابقة (أُزيل مفتاح partners_col_type2 الميت)
- Nuitka standalone: dist_nuitka/run_ui.dist/SmartAccounting.exe (143 MB — يتضمن ui.views + modules)
- Inno Setup: installer_output/SmartAccounting-Setup-v3.1.8.exe
- ZIP: installer_output/SmartAccounting-v3.1.8-win64.zip
- رُفع assets إلى GitHub release v3.1.8
- auto-update: يشير version.json إلى v3.1.8 (تاريخ 2026-08-04)
- الموقع docs/ (GitHub Pages) رُفّع: badge v3.1.8 + روابط تحميل v3.1.8 + 35 شاشة/1862 اختبار/37 وحدة + سجل v3.1.8
- التحديث عبر wscript/VBS مخفي تماماً (بدون نافذة cmd)
- ملاحظة: Smart App Control على جهاز التطوير يحجب exe غير موقّع حديث البناء — **الحل:** سكربت `tools/allow_smart_app_control.ps1` (يفحص حالة SAC + يضيف استثناء Defender) + توثيق في USER_GUIDE.md وموقع docs/ بالثلاث لغات (إيقاف SAC يدوياً اختياري أحادي الاتجاه)
- **توصية مسار العمل Git**: يُنصح بإنشاء فروع `feature/*` و `bugfix/*` عوض التطوير على `main` مباشرة — يحمي الإصدار المستقر ويُسهّل المراجعة.
