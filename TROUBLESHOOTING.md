# TROUBLESHOOTING.md
## دليل حل المشاكل الشائعة
### Smart Accounting Platform v2.5.0

---

## مشاكل التطبيق الشائعة

---

### 1. البرنامج لا يفتح

**الأعراض**: programma لا يستجيب عند النقر على run_ui.py

**الأسباب المحتملة**:
- Python غير مثبت بشكل صحيح
- البيئة الافتراضية غير مفعّلة
- ملفات ناقصة

**الحل**:
```
# 1. تأكد من تفعيل البيئة
.venv\Scripts\activate

# 2. تأكد من Python
python --version

# 3. أعد تشغيل البرنامج
python ui\run_ui.py
```

⭐ **نصيحة**: إذا ظهر خطأ `No module named`، تأكد أنك في المجلد الجذر للمشروع.

---

### 2. شاشة تسجيل الدخول لا تظهر

**الأعراض**: برنامج فارغ أو شاشة سوداء

**السبب**: خطأ في تحميل الواجهة

**الحل**:
1. أغلق البرنامج
2. احذف ملف `__pycache__` في مجلد `ui/`
3. أعد التشغيل

---

### 3. خطأ `ModuleNotFoundError`

**الأعراض**: خطأ يحدد وحدة ناقصة

**السبب**: مكتبة غير مثبّتة

**الحل**:
```
pip install -r requirements.txt
```

---

### 4. خطأ الترميز مع الحروف العربية

**الأعراض**: أخطاء `UnicodeEncodeError` مع النصوص العربية

**الحل**:
```
$env:PYTHONIOENCODING="utf-8"
python ui\run_ui.py
```

---

### 5. `PermissionError` عند حفظ الملفات

**الأعراض**: خطأ صلاحية عند حفظ التقارير أو النسخ الاحتياطي

**الحل**:
- تأكد من صلاحية الكتابة في المجلد
- أو شغّل كمسؤول (Run as Administrator)

---

### 6. البرنامج بطيء

**الأعراض**: تأخر كبير في الاستجابة

**الحل**:
1. أغلق البرامج الأخرى
2. احذف ملفات `__pycache__`
3. تأكد من كافية RAM (4GB على الأقل)

---

## مشاكل قاعدة البيانات

---

### 7. `database is locked`

**الأعراض**: خطأ `sqlite3.OperationalError: database is locked`

**السبب**: قاعدة البيانات مستخدمة من عملية أخرى

**الحل**:
1. أغلق جميع نسخ البرنامج
2. احذف ملف `accounting.db-wal` و `accounting.db-shm` إن وُجدت
3. أعد التشغيل

⭐ **نصيحة**: النظام يستخدم WAL mode، قد تحتاج لحذف ملفات auxiliary في بعض الأحيان.

---

### 8. `no such table`

**الأعراض**: خطأ beim إدخال البيانات أو عرض التقارير

**السبب**: قاعدة البيانات غير محدثة

**الحل**:
```
python -c "from database.db_operations import create_tables; create_tables()"
```

---

### 9. بيانات مفقودة

**الأعراض**: بيانات موجودة سابقاً اختفت

**الأسباب**:
- تم حذف ملف `accounting.db` بالخطأ
- النسخ الاحتياطي غير محدث

**الحل**:
1. تحقق من مجلد النسخ الاحتياطي
2. استعد من أقرب نسخة احتياطية
3. من الإعدادات: `استعادة نسخة احتياطية`

---

### 10. `FOREIGN KEY constraint failed`

**الأعراض**: خطأ عند حفظ سجل جديد

**السبب**: محاولة إدخال بيانات معتمدة على سجل غير موجود

**الحل**:
1. تأكد من إدخال البيانات الأساسية أولاً
2. تحقق من ترتيب إدخال البيانات

---

### 11. بطء استعلامات قاعدة البيانات

**الأعراض**: التقارير تستغرق وقتاً طويلاً

**الحل**:
1. شغّل `ANALYZE` على قاعدة البيانات
2. تأكد من فهرسة الجداول
3. انظر [PERFORMANCE_OPTIMIZATION.md](PERFORMANCE_OPTIMIZATION.md)

---

## مشاكل الأداء والسرعة

---

### 12. استهلاك الذاكرة مرتفع

**الأعراض**: البرنامج يستهلك RAM كثيراً بعد فترة

**السبب**: تسريب في الذاكرة (Memory Leak) من الرسوم البيانية

**الحل**:
- أعد تشغيل البرنامج كل فترة
- احذف `__pycache__` periodic

⭐ **نصيحة**: النظام يستخدم `plt.close()` لتحرير الذاكرة من الرسوم البيانية.

---

### 13. الرسوم البيانية لا تظهر

**الأعراض**: مساحة فارغة بدلاً من الرسم

**الأسباب**:
- مكتبة matplotlib غير مثبّتة
- مشكلة في Qt backend

**الحل**:
```
pip install matplotlib --upgrade
```

---

### 14. الرسوم البيانية تظهر بألوان خاطئة

**الأعراض**: ألوان غير متوقعة في الرسوم

**السبب**: تعارض بين Themed Colors

**الحل**:
- اختر مظهراً من الإعدادات (فاتح/داكن)
- أعد تشغيل البرنامج

---

## مشاكل الترجمة والعرض

---

### 15. اللغة لا تتغير عند التبديل

**الأعراض**: المحتوى يبقى بالعربية رغم تغيير اللغة

**السبب**: بعض Views لا تدعم `retranslate()` بالكامل

**الحل**:
- أعد تشغيل البرنامج بعد تغيير اللغة
- تأكد من استخدام آخر إصدار

---

### 16. النصوص العربية معكوسة

**الأعراض**: النص العربي يظهر من اليسار لليمين بشكل خاطئ

**الحل**:
- تأكد من أن اللغة مضبوطة على Arabic
- أعد تشغيل البرنامج

---

### 17. الأيقونات لا تظهر

**الأعراض**: رموز بديلاً بدلاً من الأيقونات

**السبب**: خط مدعوم غير متوفر في النظام

**الحل**:
- ثبّت خط "Segoe UI Emoji" على Windows
- أو ثبّت خط مدعوم للعربية

---

## مشاكل التقارير والتصدير

---

### 18. تصدير PDF فاشل

**الأعراض**: خطأ عند محاولة تصدير تقرير PDF

**الأسباب**:
- مكتبة fpdf2 غير مثبّتة
- مسار حفظ غير صالح
- ملف مفتوح في برنامج آخر

**الحل**:
```
pip install fpdf2 --upgrade
```
- اختر مسار حفظ مختلف
- أغلق أي ملف PDF مفتوح

---

### 19. تصدير Excel لا يعمل

**الأعراض**: لا يُنشأ ملف Excel

**السبب**: مكتبة openpyxl ناقصة

**الحل**:
```
pip install openpyxl --upgrade
```

---

### 20. التقرير يظهر بأحرف مشوهة

**الأعراض**: الحروف العربية في التقرير مشوهة أو غير مقروءة

**السبب**: خط PDF لا يدعم العربية

**الحل**:
- تأكد من توفر خط يدعم العربية (مثل "Arial")
- استخدم التصدير HTML بدلاً من PDF للنصوص العربية الطويلة

---

## جدول مرجعي سريع

| المشكلة | الحل السريع |
|---------|-----------|
| البرنامج لا يفتح | تحقق من Python + البيئة الافتراضية |
| خطأ ModuleNotFoundError | `pip install -r requirements.txt` |
| خطأ ترميز | `$env:PYTHONIOENCODING="utf-8"` |
| قاعدة بيانات مقفلة | أغلق جميع النسخ واحذف WAL files |
| البرنامج بطيء | أغلق البرامج الأخرى + احذف __pycache__ |
| لا يمكن تصدير PDF | `pip install fpdf2 --upgrade` |
| اللغة لا تتغير | أعد تشغيل البرنامج |
| ألوان خاطئة | غيّر المظهر من الإعدادات |

---

## روابط ذات صلة

- [دليل التثبيت](INSTALLATION_GUIDE.md)
- [الدليل الرئيسي](README.md)
- [النشر والتفعيل](DEPLOYMENT_GUIDE.md)
- [تحسين الأداء](PERFORMANCE_OPTIMIZATION.md)

---

*آخر تحديث: يوليو 2026*
*الإصدار: v2.5.0*
