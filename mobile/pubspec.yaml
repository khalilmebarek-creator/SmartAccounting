name: smart_accounting_mobile
description: "SmartAccounting Mobile — companion monitor for the Smart Accounting Platform."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  cryptography: ^2.9.0
  crypto: ^3.0.7
  file_picker: ^11.0.3
  path_provider: ^2.1.6
  shared_preferences: ^2.5.5
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
