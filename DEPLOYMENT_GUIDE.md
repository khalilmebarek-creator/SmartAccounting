# DEPLOYMENT_GUIDE.md
## دليل النشر والتفعيل
### Smart Accounting Platform v2.5.0

---

## 1. نشر التطبيق على أجهزة أخرى

### خيارات النشر

| الطريقة | الوصف | المناسب لـ |
|---------|-------|----------|
| نسخ المجلد | نسخ المشروع كاملاً | المطورين |
| ملف .exe | ملف مُنفّذ مستقل | المستخدمين العاديين |
| حزمة MSI | مُثبّت رسمي | الشركات الكبيرة |
| Docker | حاوية مُ conteneurizada | بيئات السيرفر |

---

### الطريقة 1: نسخ المجلد (للمطورين)

**الخطوات على الجهاز الجديد**:
1. ثبّت Python 3.10+ من [python.org](https://python.org)
2. انسخ مجلد المشروع كاملاً
3. افتح Terminal في المجلد
4. شغّل:
```
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python ui\run_ui.py
```

> ⭐ **ملاحظة**: انسخ أيضاً ملف `users.json` إذا أردت الحفاظ على المستخدمين.

---

### الطريقة 2: ملف .exe للويندوز (PyInstaller)

**الخطوة 1: تثبيت PyInstaller**
```
pip install pyinstaller
```

**الخطوة 2: إنشاء الملف**
```
pyinstaller --name "SmartAccounting" --windowed --onefile --icon=icon.ico ui\run_ui.py
```

**الخيارات**:
| الخيار | الوصف |
|--------|-------|
| `--name` | اسم الملف الناتج |
| `--windowed` | بدون نافذة cmd |
| `--onefile` | ملف واحد فقط |
| `--icon` | أيقونة مخصصة |

**الخطوة 3: نسخ الملفات المطلوبة**
يجب نسخ الملفات التالية بجانب ملف .exe:
```
SmartAccounting.exe
├── config.py
├── database/
├── modules/
├── utils/
└── ui/resources/
```

⭐ **نصيحة**: استخدم `.spec` file لتخصيص المزيد من الخيارات.

---

### الطريقة 3: حزمة MSI (WiX Toolset)

لإنشاء مُثبّت رسمي:
1. ثبّت WiX Toolset
2. أنشئ ملف `.wxs` لتعريف المكونات
3. ابني الحزمة عبر:
```
candle installer.wxs
light installer.wixobj
```

---

## 2. التوزيع على مستخدمين متعددين

### بنية المستخدمين

```
المستخدم الرئيسي (Admin)
├── مدير مالي (Manager)
├── محاسب (Accountant)
├── مُدخِل بيانات (Data Entry)
└── مُراجع (Auditor)
```

### إدارة المستخدمين

**إضافة مستخدم جديد**:
1. سجّل دخول كمدير
2. من الإعدادات → إدارة المستخدمين
3. أضف مستخدم جديد بالبريد الإلكتروني
4. حدد الصلاحية المناسبة

**حذف مستخدم**:
1. اختر المستخدم من القائمة
2. اضغط "حذف"
3. ⚠️ لا يمكن حذف المدير الأساسي

### مزامنة البيانات

**للعمل على جهاز واحد**:
- جميع المستخدمين يشتركون في نفس `accounting.db`

**للعمل على أجهزة متعددة**:
- استخدم نسخ احتياطي دوري
- أو استخدم قاعدة بيانات مشتركة (MySQL/PostgreSQL)

---

## 3. النسخ الاحتياطي والاستعادة

### إنشاء نسخة احتياطية

**من التطبيق**:
1. من الإعدادات → النسخ الاحتياطي
2. اضغط "إنشاء نسخة احتياطية"
3. اختر مكان الحفظ
4. سيُنشأ ملف مضغوط يحتوي على:
   - `accounting.db`
   - `users.json`
   - ملفات التخزين (vault)

**من Terminal (يدوي)**:
```
# Windows
copy accounting.db backup_accounting_%date:~-4%%date:~4,2%%date:~7,2%.db
copy users.json backup_users_%date:~-4%%date:~4,2%%date:~7,2%.json

# Linux/Mac
cp accounting.db "backup_accounting_$(date +%Y%m%d).db"
cp users.json "backup_users_$(date +%Y%m%d).json"
```

### استعادة نسخة احتياطية

**من التطبيق**:
1. من الإعدادات → النسخ الاحتياطي
2. اضغط "استعادة نسخة"
3. اختر ملف النسخة الاحتياطية
4. أكّد الاستعادة

⭐ **مهم**: الاستعادة ستكتب فوق البيانات الحالية. تأكد من عمل نسخة احتياطية أولاً.

### جدولة النسخ الاحتياطي التلقائي

**التوصية**:
- **يومياً**: نسخة تلقائية للمجلد المحلي
- **أسبوعياً**: نسخة للقرص الخارجي أو السحابة
- **شهرياً**: أرشيف كامل

---

## 4. الترقية من نسخة لأخرى

### قبل الترقية

1. **عمل نسخة احتياطية كاملة** ⭐
2. تحقق من إصدارك الحالي:
```
python -c "import config; print(config.APP_VERSION)"
```
3. اقرأ ملف CHANGELOG للتعديلات

### خطوات الترقية

**الخطوة 1: تحميل النسخة الجديدة**
```
git pull origin main
```
أو حمّل ملف ZIP جديد

**الخطوة 2: تحديث المكتبات**
```
.venv\Scripts\activate
pip install -r requirements.txt --upgrade
```

**الخطوة 3: تحديث قاعدة البيانات**
```
python -c "from database.db_operations import migrate; migrate()"
```

**الخطوة 4: اختبار**
```
python -m pytest tests/ -v
python ui\run_ui.py
```

**الخطوة 5: التحقق**
- سجّل دخول
- تحقق من البيانات
- جرّب التصدير

### التراجع عن الترقية

إذا واجهت مشاكل:
1. أعد تشغيل النسخة الاحتياطية
2. استرجع ملفات المشروع
3. أعد تثبيت المكتبات

---

## 5. المتطلبات على أجهزة المستخدمين

### الحد الأدنى

| المكون | المتطلب |
|--------|---------|
| نظام التشغيل | Windows 10+ |
| Python | 3.10+ |
| الرام | 4 GB |
| مساحة | 500 MB |
| الشاشة | 1280×720 |

### التوصيات

| المكون | التوصية |
|--------|---------|
| نظام التشغيل | Windows 11 |
| Python | 3.13+ |
| الرام | 8 GB+ |
| مساحة | 1 GB+ |
| الشاشة | 1920×1080+ |
| الإنترنت | Broadband |

### لملف .exe

لا يحتاج المستخدم إلى تثبيت Python:
- ملف .exe واحد مستقل
- يكفي أن ينقر عليه
- يحتاج Windows 10+ فقط

---

## 6. الإعدادات البيئية

### متغيرات البيئة المهمة

```
# ترميز النصوص
PYTHONIOENCODING=utf-8

# بيئة التطوير
QT_QPA_PLATFORM=offscreen  # للاختبار بدون شاشة

# مسار قاعدة البيانات
ACCOUNTING_DB_PATH=./accounting.db
```

### ملف config.py

```python
# الإعدادات الأساسية
APP_NAME = "Smart Accounting Platform"
APP_VERSION = "2.5.0"
DEFAULT_ADMIN_PASSWORD = "Admin@1234"

# مسارات
DB_PATH = "accounting.db"
USERS_FILE = "users.json"
BACKUP_DIR = "backups/"
```

⭐ **نصيحة أمنية**: لا تغيّر `DEFAULT_ADMIN_PASSWORD` في ملف config.py مباشرة. غيّره من واجهة التطبيق.

---

## 7. النشر في بيئة الإنتاج

### قائمة التحقق قبل النشر

- [ ] تم اختبار جميع الوحدات
- [ ] تم اختبار واجهة المستخدم يدوياً
- [ ] قاعدة البيانات مُهيأة بشكل صحيح
- [ ] النسخ الاحتياطي يعمل
- [ ] جميع المستخدمين لديهم صلاحيات مناسبة
- [ ] التقارير تعمل بشكل صحيح
- [ ] الترجمة تعمل في جميع اللغات

### بعد النشر

1. راقب سجلات الأخطاء
2. تحقق من الأداء
3. جمع ملاحظات المستخدمين
4. حدد مواعيد للصيانة الدورية

---

*آخر تحديث: يوليو 2026*
*الإصدار: v2.5.0*
