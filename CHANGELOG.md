# سجل الإصدارات — Smart Accounting Platform

> سجل الإصدارات الرسمي. يُحدَّث عند كل إصدار منشور (وسم git `vX.Y.Z` + GitHub Release + `docs/version.json`).
> الترتيب: الأحدث أولاً.

---

## v3.1.8 — 2026-08-04

- **إصدارات النظام الجبائي السنوية**: `modules/tax_years.py` + مجلد `modules/config_years/` (ملف `tax_config_<سنة>.json` لكل سنة + مؤشر `.active_year`) — تبويب «سنوات النظام» الجديد في شاشة الضرائب (مبدّل سنوات + محرر JSON مع تحقق/حفظ/استيراد/تصدير + إضافة/حذف سنة)
- **تصحيح شرائح IRG 2026** وفق نشرة DGI الرسمية: 0/23/27/30/33/35% بحدود 240k/480k/960k/1920k/3840k (القيم القديمة 0/20/30/35% بحدود 120k/360k/1440k كانت خاطئة)
- **3 أنظمة جبائية جديدة**: IFU (0.5% مقاول ذاتي / 5% إنتاج / 12% أخرى، حد أدنى 30,000 دج) + رسم التكوين المهني والتمهين (1% + 1% قابلة للخصم من ميزانية الإنفاق) + الاقتطاع من المصدر على الإيجارات (7% سكني / 15% تجاري مهني / مؤقت 7% فوق 1.8M)
- **دمج عميق** مع الإعدادات الافتراضية: ملفات السنة القديمة بلا الأقسام الجديدة تبقى صالحة تلقائياً
- **المجموع الجديد**: 1862 اختباراً ناجحاً + تغطية وحدات 100% + 1986 مفتاح ترجمة × 3 لغات | التوثيق: `docs/TAX_SYSTEM_YEARS.md`

## v3.1.7 — 2026-08-03

- **إصلاح مشاكل النسخة المثبّتة**: الشاشات الفارغة (الترقية وصلت ناقصة ملفات pandas/sqlite3/pytz) + تجمّد تحليل DuPont عند العودة + عرض 0.00 قبل الإدخال أصبح «أدخل المبلغ» + الترقية تحافظ الآن على بيانات المستخدم وكلمة السر (استثناء ملفات الحالة من المثبّت) — 1787 اختباراً ناجحاً
- **اختبار UAT شامل**: رحلة مستخدم كاملة (تسجيل دخول + تجوّل الشاشات الـ35 + إدخال بيانات + تبديل 3 لغات + حفظ + خروج) + إصلاح بغّ تبديل اللغة في شاشتي المزامنة البنكية واستيراد البيانات (`_clear_layout`)
- **تحسينات UI حسب ملاحظات المستخدم**: أسماء الألوان المترجمة في لوحة التحكم المتقدمة + تكبير جداول مراكز التكلفة والمزامنة السحابية والشركات التجريبية + حقول أوسع في اختبار المستخدمين + عزل اختبارات الواجهة
- **معالجة Smart App Control** على Windows 11: سكربت مساعد `tools/allow_smart_app_control.ps1` + توثيق بالثلاث لغات
- **إصلاح الشاشات الفارغة في المثبّتات**: التحميل الكسول لم يكن مُضمّناً في بناء Nuitka — أُضيف عبر `--include-package` وأُعيد بناء المثبّتات (مثبّت 66.9MB / ZIP محمول 109MB)
- **6 شاشات محاسبية جديدة (30-35)**: دفتر الأستاذ (F10) + حسابات الأطراف (F11) + الفواتير (F12) + المخزون (Ctrl+Shift+B) + الرواتب (Ctrl+Shift+C) + الميزانية التفصيلية (Ctrl+Shift+D)
- **طبقة تصدير موحّدة**: `ui/exporters.py` (new_workbook/add_excel_sheet) + ترحيل 3 شاشات (−180 سطر boilerplate) + 9 اختبارات
- **اختبارات أداء**: 4 اختبارات (إقلاع 44ms / مشاهد 95-402ms / ذاكرة ≤45MB) + CI عبر GitHub Actions
- **المجموع**: 1800 اختباراً ناجحاً + تغطية وحدات 100% + 1925 مفتاح ترجمة × 3 لغات

## v3.1.6 — 2026-08-01

- **تغطية واجهات شاملة**: 111 اختباراً جديداً يغطي كل الشاشات الـ35 + إصلاح بغّ ألوان جدول المعاملات في المزامنة البنكية + 44 مفتاح ترجمة مفقوداً ×3 لغات — 1772 اختباراً + تغطية وحدات 100%
- **التصحيحات النهائية + مراجعة الأمان**: إصلاح 13 خللاً (طباعة Landscape + كشف رأس ملف البنك + حماية القوالب + تصدير عربي عند غياب Amiri + تحديثات بملف جزئي آمن + استرجاع يشمل vault + رفض جداول محجوزة) + مراجعة أمان (PBKDF2 100k + تخزين مشفّر لأسرار SMTP/API + HTTPS فقط) — 1350 اختباراً
- **شاشة اختبار المستخدمين (29)**: 4 مجموعات × 5 سيناريوهات + ملاحظات بتصنيفات وأولويات + رضا + تقارير + JSON/CSV/Excel/PDF + DB
- **تجربة مستخدم محسّنة**: اختصارات كاملة (Ctrl+1..0 / F2..F8 / Ctrl+T) + قائمة «عرض» ديناميكية + رسائل خطأ موحّدة + انتقالات تلاشي + تحسينات تباين
- **التغطية الشاملة**: 1229 اختباراً + تغطية 99% (كانت 73%) — اختبارات edge/error لكل وحدة
- **اختبار التكامل والأداء**: 1266 اختباراً + إصلاح 3 أخطاء منتج (رأس المال العامل، حذف notes، نسخ احتياطي WAL)
- **تحسين أداء شامل**: إقلاع أسرع 15× (778→49ms) + ذاكرة أقل (128→45MB) + تجمّع اتصالات (حفظ 88×، قراءة 17×)
- **تعدد العملات (26)** + **المزامنة السحابية (27)** + **الشركات التجريبية (28)**

## v3.1.5 — 2026-08-01

- **تحليل ربحية مراكز التكلفة (25)**: مراكز (قسم/مشروع/فرع/خط إنتاج) + توزيع مباشر/غير مباشر (إيرادات/عدد موظفين/مساحة/متساوٍ) + ربحية + مقارنات (سابقة/ميزانية/معايير) + اتجاه + تقارير/توصيات + تصدير PDF/Excel

> **ملاحظة تتبع (Traceability note):** لم يُنشأ وسم git مستقل `v3.1.5` لأن ميزة ربحية مراكز التكلفة دخلت فعلياً في الـ commit الذي يحمل وسم `v3.1.6` (`a2e6cd2`) — أُدرج في الموقع وفي سجل التحديثات كإصدار وسيط دُمج في v3.1.6 بنفس اليوم. التوثيق الرسمي: هذا السجل + `docs/version.json` + GitHub Releases.

## v3.1.4 — 2026-07-31

- إصلاح عدم فتح التطبيق بعد التحديث + ظهور نافذة cmd عند التحديث (أصبح مخفياً تماماً)
- تحديث المثبّت إلى نسخة محسّنة وأصغر
- **المعايير المرجعية المتقدمة (18)**: أفضل الممارسات + معيار دولي + نقاط قوة/ضعف + اتجاه عبر السنوات + مقارنة منافسين
- **لوحة التحكم المتقدمة (23)**: 6 بطاقات KPI بحالة لونية + 4 رسوم + تنبيهات + تخصيص + تصدير PDF/Excel

## v3.1.3 — 2026-07-30

- 8 نسب مالية جديدة (Cash Ratio، هامش الربح التشغيلي، فترة المخزون، دوران الموردين، فترة سداد الموردين، الدورة التشغيلية، دورة التحويل النقدي، نسبة حقوق الملكية) → **20 نسبة في 4 فئات**
- 3 حقول إدخال جديدة (النقدية، المصاريف التشغيلية، متوسط الموردين)
- إصلاح الشرطة السفلية (_) في كلمة السر عند الوضع العربي

## v3.1.2 — 2026-07-28

- إصلاح التقويم الجبائي (اختيار السنة يحدّث العرض فوراً)
- إصلاح المعايير المرجعية (تغيير القطاع يحدّث النتائج تلقائياً)
- تحميل التحديث مباشرة — نافذة تقدم + تشغيل تلقائي

## v3.1.1 — 2026-07-28

- إصلاح نظام إشعار التحديثات + استعادة كلمة المرور بالرمز الآمن
- الإيميل يملأ تلقائياً بعد التسجيل + إصلاح تحميل البيانات المالية من أول مرة
- تحسين التنقل بالـ Tab بين الحقول

## v3.1.0 — 2026-07-27

- إصدار الميزات الأساسية: إدخال البيانات + لوحة التحكم + النسب + DuPont + التدقيق + التقارير
- **النظام الجبائي الجزائري**: IBS/TVA/IRG/CNAS/CNAC/VF + التقويم الجبائي + الإقرارات (G50/G57/DAS)
- **الرؤى الذكية (AI)**: تنبؤ (خطي/متوسط متحرك/تجانس أسي) + كشف شذوذ (Z-score/IQR) + توصيات
- **تحليل السيناريوهات** (مثالي/طبيعي/أسوأ + Tornado) + **Z-Score** + **التنبؤ المالي**
- **3 لغات** (AR/EN/FR) + ثيمات (فاتح/داكن/حديث) + SQLite + تصدير PDF/Excel/CSV

## v3.0.0 — 2026-07

- الإصدار الأول المنشور: منصة محاسبية بواجهة PyQt5 (الشاشات 1-20) + دعم اللغة العربية (RTL)
- قاعدة بيانات SQLite + تقارير + استيراد Excel/CSV

---

*صيغة السجل وفق Keep a Changelog — تُحدَّث عند كل إصدار منشور مع وسم git مطابق.*
